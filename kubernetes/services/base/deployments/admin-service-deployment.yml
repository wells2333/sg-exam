apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
        version: latest
    spec:
      containers:
        - name: admin-service
          image: registry.cn-hangzhou.aliyuncs.com/sg-exam/admin-service
          imagePullPolicy: Never
          ports:
            - containerPort: 80