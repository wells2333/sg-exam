apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: ui-service-vs
spec:
  hosts:
    - "*"
  gateways:
    - sg-gateway
  http:
    - match:
        - uri:
            prefix: /admin
      route:
        - destination:
            port:
              number: 80
            host: ui-service.sg-exam.svc.cluster.local
