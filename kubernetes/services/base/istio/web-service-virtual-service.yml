apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: web-service-vs
spec:
  hosts:
    - "*"
  gateways:
    - sg-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            port:
              number: 80
            host: web-service.sg-exam.svc.cluster.local
