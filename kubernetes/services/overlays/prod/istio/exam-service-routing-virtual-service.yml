apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: exam-service-vs
spec:
  http:
    - match:
        - uri:
            prefix: /exam-service
      route:
        - destination:
            host: exam-service
            subset: old
          weight: 100
        - destination:
            host: exam-service
            subset: new
          weight: 0