apiVersion: apps/v1
kind: Deployment
metadata:
  name: exam-service
spec:
  selector:
    matchLabels:
      app: exam-service
  template:
    metadata:
      labels:
        app: exam-service
        version: v2
    spec:
      containers:
        - name: exam-service
          image: registry.cn-hangzhou.aliyuncs.com/sg-exam/exam-service:v2
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker,prod"
          resources:
            limits:
              memory: 512Mi
